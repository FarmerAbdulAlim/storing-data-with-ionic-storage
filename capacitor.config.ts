import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'storing-data-with-ionic-storage',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
